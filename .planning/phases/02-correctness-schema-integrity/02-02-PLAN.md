---
phase: 02-correctness-schema-integrity
plan: "02"
type: execute
wave: 2
depends_on:
  - "02-01"
files_modified:
  - worker/src/schema.ts
  - scripts/check-schema-drift.ts
  - package.json
  - AGENTS.md
autonomous: true
requirements:
  - REQ-SCHEMA-01
user_setup: []
must_haves:
  truths:
    - "pnpm check:schema-drift exits 0 when films, scenes, shots, shotSemantic, shotEmbeddings SQL columns match between app and worker schema"
    - "shot_metadata divergence is reported as a documented warning, not a silent mismatch"
  artifacts:
    - "scripts/check-schema-drift.ts"
    - "package.json script check:schema-drift"
  key_links:
    - "src/db/schema.ts ↔ worker/src/schema.ts"
---

<objective>
**(1)** Align worker **`films`** table with app schema for **`sourceUrl`** (PostgreSQL column `source_url`) so shared tables match where the worker reads/writes `films`.

**(2)** Add **`scripts/check-schema-drift.ts`** (run via `tsx`) that compares **SQL column names** for `films`, `scenes`, `shots`, `shotSemantic`, `shot_embeddings` between `src/db/schema.ts` and `worker/src/schema.ts` using Drizzle **`getTableColumns`** from `drizzle-orm`.

**(3)** For **`shot_metadata`**, do **not** fail the check: print a clear **warning** that app vs worker definitions differ by design until worker ingest is migrated to the composition-based columns—reference `.planning/codebase/CONCERNS.md`.

**(4)** Register **`check:schema-drift`** in root `package.json` and document in `AGENTS.md` (Commands or Key Files).
</objective>

<execution_context>
@.cursor/get-shit-done/workflows/execute-plan.md
@.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@src/db/schema.ts
@worker/src/schema.ts
@.planning/codebase/CONCERNS.md (dual schema / worker drift)

**Script sketch:** import `getTableColumns` from `drizzle-orm`; import `* as app` from `../src/db/schema.ts`; import `* as worker` from `../worker/src/schema.ts` (paths must work with `tsx` from repo root). Build sorted arrays of SQL names via `Object.values(getTableColumns(table)).map((c) => c.name).sort()`. Compare for equality per table. Exit code **1** if any compared table differs; exit **0** if all match. Always stderr **warn** for `shot_metadata` with the CONCERNS reference.

**Worker `films`:** add `sourceUrl: text("source_url")` to mirror `src/db/schema.ts` `films` row (same snake_case column names as app).
</context>

<threat_model>
Schema drift detection prevents silent DB shape mismatches. No new runtime attack surface.
</threat_model>

<tasks>

<task type="auto">
  <name>Add sourceUrl to worker films table</name>
  <files>worker/src/schema.ts</files>
  <read_first>src/db/schema.ts (films pgTable), worker/src/schema.ts</read_first>
  <action>Add `sourceUrl: text("source_url"),` to `films` in `worker/src/schema.ts` in the same logical order as `src/db/schema.ts` (after `genres`, before `createdAt`).</action>
  <verify>cd worker && pnpm build</verify>
  <acceptance_criteria>
    - `worker/src/schema.ts` contains `source_url` or `sourceUrl` mapping to column `source_url`
    - Worker `pnpm build` exits 0
  </acceptance_criteria>
  <done>Worker films row matches app column set for shared fields</done>
</task>

<task type="auto">
  <name>Add schema drift check script</name>
  <files>scripts/check-schema-drift.ts</files>
  <read_first>src/db/schema.ts, worker/src/schema.ts</read_first>
  <action>Create `scripts/check-schema-drift.ts` implementing the comparison described in this PLAN file’s context section. Compared tables: `films`, `scenes`, `shots`, `shotSemantic`, `shotEmbeddings`. Warn-only block for `shotMetadata` / `shot_metadata`. Use `process.exit(1)` on mismatch for compared tables.</action>
  <verify>pnpm check:schema-drift</verify>
  <acceptance_criteria>
    - File `scripts/check-schema-drift.ts` exists
    - Root `package.json` has `"check:schema-drift": "tsx scripts/check-schema-drift.ts"`
    - `pnpm check:schema-drift` exits 0 after worker `films` alignment
  </acceptance_criteria>
  <done>Automated drift detection in place</done>
</task>

<task type="auto">
  <name>Document check in AGENTS.md</name>
  <files>AGENTS.md</files>
  <read_first>AGENTS.md</read_first>
  <action>Add under Commands: `pnpm check:schema-drift` — Verify app vs worker Drizzle schema alignment for shared tables (see `scripts/check-schema-drift.ts`).</action>
  <verify>grep check:schema-drift AGENTS.md</verify>
  <acceptance_criteria>
    - `AGENTS.md` mentions `check:schema-drift`
  </acceptance_criteria>
  <done>Operators discover the check</done>
</task>

</tasks>

<verification>
- [ ] `pnpm build` at repo root passes
- [ ] `pnpm check:schema-drift` exits 0
- [ ] `cd worker && pnpm build` passes
</verification>

<success_criteria>
- REQ-SCHEMA-01 partially met: automation catches shared-table drift; `shot_metadata` gap explicit until ingest migration
</success_criteria>

<output>
After completion, create `.planning/phases/02-correctness-schema-integrity/02-02-SUMMARY.md`
</output>
