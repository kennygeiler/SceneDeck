---
phase: 02-correctness-schema-integrity
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/api-auth.ts
autonomous: true
requirements:
  - REQ-CORR-01
user_setup: []
must_haves:
  truths:
    - "generateApiKey uses randomUUID from node:crypto"
    - "No reference to global crypto in api-auth.ts for UUID generation"
  artifacts:
    - "src/lib/api-auth.ts updated imports"
  key_links:
    - "CONCERNS Known Bugs — api-auth crypto"
---

<objective>
Fix `src/lib/api-auth.ts` so `generateApiKey` uses **`import { createHash, randomUUID } from "node:crypto"`** and `randomUUID()` instead of `crypto.randomUUID()`, removing reliance on an ambient global `crypto` binding (runtime-dependent; TS may not model it).
</objective>

<execution_context>
@.cursor/get-shit-done/workflows/execute-plan.md
@.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/codebase/CONCERNS.md (Known Bugs — generateApiKey)
@src/lib/api-auth.ts

Current line to replace: `const raw = \`mv_${crypto.randomUUID().replace(/-/g, "")}\`;`
Target: `const raw = \`mv_${randomUUID().replace(/-/g, "")}\`;`
</context>

<threat_model>
API key generation uses cryptographically sound UUIDs from Node's implementation. No new trust boundaries.
</threat_model>

<tasks>

<task type="auto">
  <name>Use node:crypto randomUUID in generateApiKey</name>
  <files>src/lib/api-auth.ts</files>
  <read_first>src/lib/api-auth.ts</read_first>
  <action>Change line 6 from `import { createHash } from "node:crypto"` to `import { createHash, randomUUID } from "node:crypto"`. In `generateApiKey`, replace `crypto.randomUUID()` with `randomUUID()`. Ensure no remaining `crypto.` reference in this file.</action>
  <verify>pnpm build</verify>
  <acceptance_criteria>
    - `src/lib/api-auth.ts` contains `import { createHash, randomUUID } from "node:crypto"`
    - `src/lib/api-auth.ts` contains `randomUUID()` inside `generateApiKey` (grep for `randomUUID`)
    - `grep -n 'crypto\\.randomUUID\\|globalThis.crypto' src/lib/api-auth.ts` exits 1 (no matches)
  </acceptance_criteria>
  <done>UUID generation is explicit Node API</done>
</task>

</tasks>

<verification>
- [ ] `pnpm build` passes
</verification>

<success_criteria>
- REQ-CORR-01 satisfied; CONCERNS api-auth bullet addressed
</success_criteria>

<output>
After completion, create `.planning/phases/02-correctness-schema-integrity/02-01-SUMMARY.md`
</output>
