---
phase: 10-shot-boundary-cut-eval-and-tuning-product
plan: "02"
type: plan
wave: 2
depends_on:
  - "01"
files_modified: []
autonomous: false
requirements:
  - REQ-BT-03
---

<objective>
Ship **operator UI** for global presets, gold revision history, eval runs (metrics + misses), and **apply preset to ingest** (or guided handoff), extending **`/tuning`** and/or **`/eval/gold-annotate`** without conflating **classification** settings.
</objective>

<task id="10-02-01">
<title>Tuning workspace UI</title>
<read_first>
- `src/app/(site)/tuning/page.tsx`
- `src/components/eval/gold-annotate-workspace.tsx` (patterns)
- Phase 10 APIs from `10-01-PLAN.md`
</read_first>
<action>
Add a **tuning workspace** route (e.g. `/tuning/workspace` or tabs on `/tuning`):

1. **Presets** — List global presets; show JSON summary (read-only expandable); “duplicate as new preset” (optional v1).
2. **Gold** — Film picker + window; link to gold annotate or embedded flow; **revision timeline** (list prior revisions, open diff of `cutsSec` counts / timestamps).
3. **Run eval** — Select gold revision + preset → trigger worker job → poll → show **P/R/F1**, **FN/FP** table or download JSON.
4. **Apply** — Set **`boundaryCutPresetId`** on film (if column added) or show **copy-paste env block** generated from preset for worker deploy.

Copy must state **classification is global** and not configured here.
</action>
<acceptance_criteria>
- `10-VALIDATION.md` UAT items 1–5 achievable through UI without CLI (except where worker URL must be configured).
</acceptance_criteria>
</task>

<task id="10-02-02">
<title>Film ↔ preset association (optional in wave 2)</title>
<read_first>
- `src/db/schema.ts` (`films` table)
</read_first>
<action>
If not done in 10-01: add nullable **`boundaryCutPresetId`** on `films` (FK → `boundary_cut_presets`). Ingest worker reads film row when starting job and applies preset **unless** request body overrides.

Update film detail or ingest UI to display **active cut preset**.
</action>
<acceptance_criteria>
- Ingest with film that has preset id uses that preset without operator setting env vars manually.
</acceptance_criteria>
</task>

<task id="10-02-03">
<title>Docs and onboarding</title>
<read_first>
- `docs/tuning-flow.md`, `eval/runs/STATUS.md`, `README.md`
</read_first>
<action>
Update **`docs/tuning-flow.md`** “Target in-app” column to point at Phase 10 deliverables. Add short **operator quickstart** (preset → gold revision → eval run → ingest).

Link **`/tuning`** from landing or header if product wants discoverability (optional).
</action>
<acceptance_criteria>
- New operator can follow docs only to complete one eval cycle on a dev worker.
</acceptance_criteria>
</task>

## must_haves

- UI strings never imply users are tuning **Gemini classification**; use **boundary / cut** language only.
