---
phase: 10-shot-boundary-cut-eval-and-tuning-product
plan: "01"
type: plan
wave: 1
depends_on: []
files_modified: []
autonomous: false
requirements:
  - REQ-BT-01
  - REQ-BT-02
---

<objective>
Add **persisted global boundary cut presets**, **versioned gold revisions** per film/window, and **server-side eval run** records with provenance, plus **worker (or job) entry point** that runs `detectShotsForIngest` (or equivalent) using **preset-derived options** instead of only ambient env.
</objective>

## Context

- **Research:** `10-RESEARCH.md` — presets global; gold editable with history; no cost caps v1; classification out of scope.
- **Existing:** `eval_artifacts`, `eval/gold-annotate`, `docs/tuning-flow.md`, `src/lib/ingest-pipeline.ts`, `src/lib/boundary-ensemble.ts`, `src/lib/boundary-fusion.ts`.

<task id="10-01-01">
<title>Schema: global `boundary_cut_presets`</title>
<read_first>
- `src/db/schema.ts` (`evalArtifacts`, `films`, patterns for jsonb)
- `src/lib/boundary-ensemble.ts`, `src/lib/ingest-pipeline.ts` (`DetectShotsForIngestOptions`)
</read_first>
<action>
Add a Drizzle table e.g. **`boundary_cut_presets`**:

- `id` (uuid PK), `name` (text, human label), `slug` (optional unique), `description` (optional text)
- **`config` (jsonb)** — typed shape mirroring what ingest needs: `boundaryDetector` string, `mergeGapSec` number, `fusionPolicy`, optional `detector` (`content` | `adaptive`) when relevant, flags for extras (no secrets in JSON — references only)
- `isArchived` (boolean, default false), `createdAt`, `updatedAt`

Add migration / `pnpm db:generate`. Seed one row matching **CEMENTED** row in `eval/runs/STATUS.md` (optional seed script or SQL note in plan execution).

Export types from schema; add **`presetConfigToDetectOptions()`** (or similar) in `src/lib/` mapping JSON → `detectShotsForIngest` options + env override strategy **documented** (child process env vs in-process override — prefer explicit override API in `detectShotsForIngest` if missing today).
</action>
<acceptance_criteria>
- `pnpm check:schema-drift` passes after migration.
- TypeScript type for `config` documented in one module (Zod schema or interface + comment).
</acceptance_criteria>
</task>

<task id="10-01-02">
<title>Schema: gold revisions (history)</title>
<read_first>
- `eval_artifacts` usage in `src/app/api/eval/artifacts/**`
- `src/lib/eval-cut-json.ts`
</read_first>
<action>
Choose one approach (document in migration comment):

- **A)** New table **`eval_gold_revisions`**: `id`, `filmId`, `windowStartSec`, `windowEndSec` (nullable = full film), `payload` (jsonb with `cutsSec` + notes), `replacesRevisionId` (nullable self-FK), `createdBy` (optional text), `createdAt`; **current** = head of chain or latest `createdAt` per `(filmId, window)`.

- **B)** Extend **`eval_artifacts`** with `kind = gold_revision`, `replacesArtifactId`, enforce append-only updates via API.

Implement **A** unless eval_artifacts already covers 90% — prefer normalized revision table for queries.

API: `POST` create revision, `GET` list history for film+window, `GET` single revision by id.
</action>
<acceptance_criteria>
- Creating two revisions for same film/window yields two rows; first remains readable.
- Extracted `cutsSec` passes `extractCutsSecFromEvalJson` / same shape as file gold.
</acceptance_criteria>
</task>

<task id="10-01-03">
<title>Eval run record + score API</title>
<read_first>
- `src/lib/boundary-eval.ts` (`evalBoundaryCuts`)
- `scripts/eval-pipeline.ts` (metric definitions)
</read_first>
<action>
Add **`boundary_eval_runs`** (or reuse artifacts with `kind=boundary_eval_run`): links `goldRevisionId`, `presetId`, `predictedPayload` or artifact ref, `toleranceSec`, `metrics` jsonb `{ precision, recall, f1, tp, fp, fn }`, `unmatchedGoldSec`, `unmatchedPredSec` (jsonb arrays), `provenance` jsonb, `createdAt`.

Implement **POST** “run eval” that: loads gold + runs detect job (10-01-04) **or** accepts pre-uploaded predicted artifact id; computes metrics via shared `evalBoundaryCuts`; persists row.

Implement **GET** by id and list by `filmId`.
</action>
<acceptance_criteria>
- For a fixed gold + pred JSON pair, API F1 matches `pnpm eval:pipeline` within float tolerance.
</acceptance_criteria>
</task>

<task id="10-01-04">
<title>Worker: detect job with preset override</title>
<read_first>
- `worker/src/ingest.ts`, `src/lib/ingest-pipeline.ts`
- `scripts/detect-export-cuts.ts`
</read_first>
<action>
Add a **dedicated job** or **ingest submode**: input = `{ filmId | videoPath, presetId | inlinePresetConfig, window, jobId }`. Worker loads preset, applies **explicit** boundary options to `detectShotsForIngest` (refactor ingest-pipeline if env is the only hook today — **thread options through** without mutating `process.env` globally when possible).

Persist predicted cuts to S3 or DB artifact; return job status via existing SSE/poll pattern.

Document operator requirements (FFmpeg, scenedetect PATH) in `docs/tuning-flow.md`.
</action>
<acceptance_criteria>
- Running the same preset against the same clip twice yields deterministic `cutsSec` (modulo detector nondeterminism documented).
- No change to classification pipeline when only boundary job runs.
</acceptance_criteria>
</task>

## must_haves

- Classification path **unchanged** for this plan’s PRs unless a shared type import forces a no-op re-export.
- `AGENTS.md` updated: new tables, preset concept, gold revisions, eval run API names.
