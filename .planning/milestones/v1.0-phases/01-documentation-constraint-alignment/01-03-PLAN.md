---
phase: 01-documentation-constraint-alignment
plan: "03"
type: execute
wave: 4
depends_on:
  - "01-04"
files_modified:
  - package.json
  - src/db/seed.ts
  - AGENTS.md
  - README.md
autonomous: true
requirements:
  - REQ-DOC-01
user_setup:
  - "DATABASE_URL in .env.local for optional manual seed run (executor may skip run if CI has no DB)"
must_haves:
  truths:
    - "package.json defines script db:seed that runs tsx src/db/seed.ts"
    - "src/db/seed.ts exists and inserts at least one deterministic dev row or no-ops with clear log"
  artifacts:
    - "src/db/seed.ts"
  key_links:
    - "AGENTS.md db:seed ↔ package.json script"
---

<objective>
Make the documented `pnpm db:seed` command real: add `db:seed` to root `package.json` and implement `src/db/seed.ts` using the existing `db` export from `src/db/index.ts`. **Depends on 01-04** so `package.json` name/metadata edits happen first, then this plan adds the script line.
</objective>

<execution_context>
@.cursor/get-shit-done/workflows/execute-plan.md
@.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@src/db/schema.ts (films table minimum columns)
@src/db/index.ts
@AGENTS.md
@package.json

**Seed behavior:** Insert one film row with `title: "Seed (dev)"`, `director: "MetroVision"`, `year: 1970` **only if** no film has that title (use `db.insert(films).values(...).onConflictDoNothing()` is invalid without unique—use select-first: `const existing = await db.select().from(films).where(eq(films.title, "Seed (dev)")).limit(1)` then insert if empty). Use `import { db } from "./index"` or `from "./index.js"` per project ESM—match `generate-embeddings.ts` import style.
</context>

<threat_model>
Seed runs against operator DATABASE_URL; no new public endpoints. Seed must not log secrets.
</threat_model>

<tasks>

<task type="auto">
  <name>Add db:seed script</name>
  <files>package.json</files>
  <read_first>package.json</read_first>
  <action>Add to `"scripts"`: `"db:seed": "tsx src/db/seed.ts"` next to other `db:*` scripts. Preserve JSON order style used in file (alphabetical or existing db:* grouping).</action>
  <verify>node -e "JSON.parse(require('fs').readFileSync('package.json','utf8')).scripts['db:seed']"</verify>
  <acceptance_criteria>
    - `package.json` contains the exact key `"db:seed"` with value containing `tsx src/db/seed.ts`
  </acceptance_criteria>
  <done>Script registered</done>
</task>

<task type="auto">
  <name>Implement src/db/seed.ts</name>
  <files>src/db/seed.ts</files>
  <read_first>src/db/schema.ts, src/db/index.ts, src/db/generate-embeddings.ts (import pattern)</read_first>
  <action>Create `src/db/seed.ts` that: imports `db` from `./index`, `films` and `eq` from schema/drizzle, checks for existing title `Seed (dev)`, inserts if absent, `console.log` success or skip. Call `process.exit(0)` at end. Handle errors with `console.error` and `process.exit(1)`.</action>
  <verify>pnpm exec tsc --noEmit 2>/dev/null || pnpm build — must pass</verify>
  <acceptance_criteria>
    - File `src/db/seed.ts` exists
    - `src/db/seed.ts` contains `Seed (dev)` and `from "./index"` or equivalent path used elsewhere in src/db
    - `src/db/seed.ts` uses `db` from project singleton pattern
  </acceptance_criteria>
  <done>Idempotent dev seed implemented</done>
</task>

<task type="auto">
  <name>Align README if it references seed</name>
  <files>README.md</files>
  <read_first>README.md</read_first>
  <action>If README documents database setup, add `pnpm db:push` then `pnpm db:seed` in Quick Start only if accurate; fix `cd Claude` typo to repo name if present in same edit pass.</action>
  <verify>optional — README may not mention seed</verify>
  <acceptance_criteria>
    - If README contains `db:seed`, it matches `package.json` script name exactly
  </acceptance_criteria>
  <done>User-facing quickstart honest</done>
</task>

</tasks>

<verification>
- [ ] `pnpm db:seed` exits 0 with DATABASE_URL set (manual); without DB, at least `tsx --eval` import check passes
</verification>

<success_criteria>
- AGENTS.md `pnpm db:seed` is executable
</success_criteria>

<output>
After completion, create `01-03-SUMMARY.md`
</output>
