---
phase: 01-documentation-constraint-alignment
plan: "02"
type: execute
wave: 2
depends_on:
  - "01-01"
files_modified:
  - .kiln/docs/arch-constraints.md
  - .kiln/docs/pitfalls.md
  - AGENTS.md
  - .kiln/docs/tech-stack.md
  - .kiln/validation/architecture-check.md
autonomous: true
requirements:
  - REQ-DOC-01
user_setup: []
must_haves:
  truths:
    - "AC-14 and AGENTS.md describe the same drizzle-orm major line as package.json (^0.45.1)"
    - "No doc claims pin ~0.38.x while package.json uses ^0.45.1 without an explicit exception note"
  artifacts:
    - "ARCHITECTURE / pitfall docs updated for 0.45.x builder API notes if needed"
  key_links:
    - "package.json drizzle-orm ↔ AC-14"
---

<objective>
Eliminate split brain on Drizzle ORM: the repo runs `drizzle-orm@^0.45.1` (root and worker). Update **AC-14**, **AGENTS.md**, **pitfalls**, and any Kiln validation snippets that still mandate `~0.38.x`.
</objective>

<execution_context>
@.cursor/get-shit-done/workflows/execute-plan.md
@.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@package.json
@worker/package.json
@.kiln/docs/arch-constraints.md
@AGENTS.md
@.planning/codebase/CONCERNS.md

Chosen policy (locked for this phase): **Document truth in code** — keep `^0.45.1`, update constraints and agent guidance. Add one sentence that AC-14 originally targeted 0.38.x but the project standard is now 0.45.x with builder API (`db.select().from().where()`).
</context>

<threat_model>
Documentation alignment only. Accurate version pins reduce supply-derived exploit surface from mistaken downgrades.
</threat_model>

<tasks>

<task type="auto">
  <name>Update AC-14 and related Kiln docs</name>
  <files>.kiln/docs/arch-constraints.md,.kiln/docs/pitfalls.md,.kiln/docs/tech-stack.md,.kiln/validation/architecture-check.md</files>
  <read_first>.kiln/docs/arch-constraints.md (AC-14 block), .kiln/docs/pitfalls.md (Drizzle section), package.json dependencies</read_first>
  <action>Rewrite AC-14 to: pin `drizzle-orm` to `^0.45.1` (match `package.json` and `worker/package.json` **exactly**). Remove mandatory `~0.38.x`. Retain “prefer builder API over db.query.*” guidance. In `.kiln/docs/pitfalls.md`, replace “Lock drizzle-orm to ~0.38.x” with the same `^0.45.1` policy. In `.kiln/validation/architecture-check.md`, fix any line claiming 0.38 pin as the requirement—state 0.45.1 satisfies the constraint. Update `tech-stack.md` Drizzle row to `^0.45.1` with note “Neon + drizzle-kit 0.31.x”.</action>
  <verify>rg "~0\.38" .kiln docs AGENTS.md 2>/dev/null; should return no AC-14-style mandates (historical footnotes in ADR-style allowed only in a quoted “superseded” box—prefer zero remaining ~0.38 mandates)</verify>
  <acceptance_criteria>
    - `.kiln/docs/arch-constraints.md` contains `^0.45.1` or `0.45.1` in the AC-14 constraint body and does not contain `~0.38.x` as the active pin
    - `.kiln/docs/pitfalls.md` does not contain `~0.38.x` as the prevention rule for Drizzle (grep -F "~0.38" .kiln/docs/pitfalls.md exits 1)
  </acceptance_criteria>
  <done>AC-14 matches lockfiles/package.json intent</done>
</task>

<task type="auto">
  <name>Update AGENTS.md Drizzle line and Known Issues</name>
  <files>AGENTS.md</files>
  <read_first>AGENTS.md, .kiln/docs/arch-constraints.md (updated AC-14)</read_first>
  <action>In **Conventions → Database**, replace `Pin drizzle-orm to ~0.38.x (AC-14)` with `Use drizzle-orm ^0.45.1 (AC-14); prefer builder API db.select().from().where().` Remove the Known Issues bullet about drizzle-orm intentional upgrade vs arch-constraints if present, or rewrite to “constraint docs aligned in Phase 01.”</action>
  <verify>grep -n "drizzle" AGENTS.md</verify>
  <acceptance_criteria>
    - `AGENTS.md` contains `^0.45.1` or `0.45.1` in the Database convention line
    - `AGENTS.md` does not contain `~0.38.x` as an active instruction
  </acceptance_criteria>
  <done>AGENTS matches AC-14</done>
</task>

</tasks>

<verification>
- [ ] `pnpm build` still passes (no code change expected; run if any TS-adjacent doc broke)
</verification>

<success_criteria>
- Single Drizzle version story across Kiln + AGENTS.md + package.json
</success_criteria>

<output>
After completion, create `01-02-SUMMARY.md`
</output>
