---
phase: 01-documentation-constraint-alignment
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - .kiln/docs/codebase-state.md
  - .kiln/master-plan.md
  - .kiln/docs/arch-constraints.md
  - .kiln/docs/patterns.md
  - .kiln/docs/decisions.md
  - .kiln/docs/research.md
  - .kiln/docs/research/pipeline-canonicalization.md
  - .kiln/plans/plan_validation.md
  - .kiln/plans/codex_plan.md
  - .kiln/validation/architecture-check.md
autonomous: true
requirements:
  - REQ-DOC-01
user_setup: []
must_haves:
  truths:
    - "No Kiln doc claims src/app/api/detect-shots/route.ts exists unless the file is present"
    - "API route inventory under .kiln lists only segments that exist under src/app/api/"
  artifacts:
    - ".kiln/docs/codebase-state.md reflects current package.json (no phantom bullmq/ioredis/@vercel/blob if absent)"
  key_links:
    - "CONCERNS.md trace AC-23 ↔ Kiln narrative"
---

<objective>
Reconcile Kiln documentation with the live Next.js API tree and remove stale **detect-shots** references. Update AC-23 wording where it still implies the old shell-out route exists.
</objective>

<execution_context>
@.cursor/get-shit-done/workflows/execute-plan.md
@.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/codebase/CONCERNS.md

Live API segments (verify with `find src/app/api -name route.ts`): `agent/chat`, `batch/{source,submit,status,review}`, `detect-objects`, `export`, `group-scenes`, `ingest-film`, `ingest-film/stream`, `process-scene`, `rag`, `s3`, `search`, `shots`, `upload-to-s3`, `upload-video`, `verifications`, `verifications/[shotId]`, `admin/accuracy`, `admin/correction-patterns`, `v1/films`, `v1/search`, `v1/shots`, `v1/taxonomy`.
**There is no** `src/app/api/detect-shots/route.ts` or `blob/[...path]` in the current tree (confirm with Glob before editing).
</context>

<threat_model>
Documentation-only. Risk: misleading operators about attack surface—fixing reduces false sense of missing/present routes. No new executable code.
</threat_model>

<tasks>

<task type="auto">
  <name>Inventory and patch codebase-state.md</name>
  <files>.kiln/docs/codebase-state.md</files>
  <read_first>src/app/api/**/route.ts (list via glob), package.json, worker/package.json, .planning/codebase/CONCERNS.md</read_first>
  <action>Update `.kiln/docs/codebase-state.md`: (1) Mark AC-23 / detect-shots deliverable as satisfied **only** if you state clearly that the Next.js route is **absent** and interactive ingest uses `ingest-film/stream` → worker (match `.kiln/docs/patterns.md` PF rule). Remove any checkbox that says `detect-shots/route.ts still exists`. (2) Replace the **API Routes** bullet list with the actual route segments from the repo (no `detect-shots`, no `blob/[...path]` unless the file exists). (3) Fix **DB** inventory line: remove `seed.ts` from the list unless `src/db/seed.ts` exists after Phase plan 01-03. If 01-03 not run yet, change to “seed.ts — not present (see AGENTS.md / Phase 01-03)”. (4) Update bullmq/ioredis/@vercel/blob deliverables to match root `package.json` (grep shows they are absent—mark removed or N/A). (5) Update package name bullets: root `name` is `metrovision`; align worker name with `worker/package.json` field `name` exactly.</action>
  <verify>rg "detect-shots/route" .kiln/docs/codebase-state.md — should be 0 or only historical past-tense; rg "blob/\[\.\.\.path\]" .kiln/docs/codebase-state.md — 0 if route missing</verify>
  <acceptance_criteria>
    - `.kiln/docs/codebase-state.md` does not contain the substring `src/app/api/detect-shots/route.ts still exists`
    - `.kiln/docs/codebase-state.md` API inventory does not list `detect-shots` as a current route unless `src/app/api/detect-shots/route.ts` exists on disk
    - `grep -E "bullmq|ioredis|@vercel/blob" package.json` returns empty AND deliverables in codebase-state do not claim these packages are still required removals if already gone (word as “removed” or strike through context)
  </acceptance_criteria>
  <done>codebase-state TL;DR matches tree; stale routes removed from inventory</done>
</task>

<task type="auto">
  <name>Align master-plan and AC-23 references</name>
  <files>.kiln/master-plan.md,.kiln/docs/arch-constraints.md,.kiln/docs/patterns.md,.kiln/docs/decisions.md,.kiln/docs/research.md,.kiln/docs/research/pipeline-canonicalization.md,.kiln/plans/plan_validation.md,.kiln/plans/codex_plan.md,.kiln/validation/architecture-check.md</files>
  <read_first>.kiln/docs/codebase-state.md (after task 1), .kiln/docs/arch-constraints.md section AC-23</read_first>
  <action>In each file found by `rg -l "detect-shots" .kiln --glob '!**/node_modules/**'`: Update narrative so AC-23 reads as **retired/absent** Next.js shell-out route; canonical interactive path is TS worker + `ingest-film/stream`. Remove imperative “consolidate route” language that implies the file still exists. For `arch-constraints.md` AC-23: replace “must be consolidated” with **current constraint**: e.g. “The Next.js `detect-shots` route MUST NOT be reintroduced; interactive detection uses the worker.” Ensure `master-plan.md` checklist items for M1 don’t claim the file still exists.</action>
  <verify>rg "still exists" .kiln --glob "*.md" | rg detect-shots ; expect exit 1 (no matches)</verify>
  <acceptance_criteria>
    - `rg "detect-shots.*still" .kiln -g'*.md'` returns no matches
    - `.kiln/docs/arch-constraints.md` AC-23 body references worker/`ingest-film` canonical path and does not require consolidating a present `src/app/api/detect-shots/route.ts`
  </acceptance_criteria>
  <done>AC-23 docs consistent with retired route</done>
</task>

</tasks>

<verification>
- [ ] `test ! -f src/app/api/detect-shots/route.ts` (expect true) and docs state retirement, not “still exists”
- [ ] `rg -n "API Routes" .kiln/docs/codebase-state.md` shows list without phantom segments
</verification>

<success_criteria>
- Kiln docs match `src/app/api/**` inventory
- AC-23 narrative is factually true in the repository today
</success_criteria>

<output>
After completion, create `.planning/phases/01-documentation-constraint-alignment/01-01-SUMMARY.md`
</output>
