---
phase: 01-documentation-constraint-alignment
plan: "04"
type: execute
wave: 3
depends_on:
  - "01-02"
files_modified:
  - package.json
  - worker/package.json
  - AGENTS.md
  - README.md
  - .kiln/docs/codebase-state.md
autonomous: true
requirements:
  - REQ-DOC-01
user_setup: []
must_haves:
  truths:
    - "README and AGENTS state a single canonical product name rule (MetroVision primary; SceneDeck as alternate/historical if applicable)"
    - "worker package name documents metrovision-worker or matches actual worker package.json name field"
  artifacts:
    - "README title section updated"
  key_links:
    - "package.json name ↔ AGENTS Architecture TL;DR"
---

<objective>
Align naming: root **`package.json`** is already `"metrovision"`; **README** still says SceneDeck. **worker/package.json** may still say `scenedeck-worker`. Establish one explicit convention in AGENTS + README + Kiln state.
</objective>

<execution_context>
@.cursor/get-shit-done/workflows/execute-plan.md
@.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@package.json
@worker/package.json
@AGENTS.md
@README.md

**Target convention (concrete):** Title style: **MetroVision** with parenthetical *(SceneDeck)* as legacy/repo codename where helpful—mirror existing `AGENTS.md` Architecture line: `MetroVision (SceneDeck)`.
**Worker:** `worker/package.json` should have `"metrovision-worker"`. If it still says `scenedeck-worker`, rename; if already `metrovision-worker`, only update docs that claim otherwise.
</context>

<threat_model>
Naming only. No security impact.
</threat_model>

<tasks>

<task type="auto">
  <name>Normalize package names</name>
  <files>package.json,worker/package.json</files>
  <read_first>package.json, worker/package.json</read_first>
  <action>Ensure root `name` is `metrovision`. Set worker `name` to `metrovision-worker` (if different, change it). Do not rename npm scope fields if absent.</action>
  <verify>grep '"name"' package.json worker/package.json</verify>
  <acceptance_criteria>
    - Root `package.json` `"name"` is `metrovision`
    - `worker/package.json` `"name"` is `metrovision-worker`
  </acceptance_criteria>
  <done>Package names aligned</done>
</task>

<task type="auto">
  <name>Update README hero title</name>
  <files>README.md</files>
  <read_first>README.md, AGENTS.md (first Architecture paragraph)</read_first>
  <action>Change README H1 from `# SceneDeck` to `# MetroVision (SceneDeck)` or `# MetroVision` with a subtitle line “Previously: SceneDeck codename / repo scenedeck”. Add one sentence under title matching AGENTS product description. Fix Quick Start `cd Claude` → `cd SceneDeck` or actual folder name placeholder `cd metrovision` → use generic `cd <repo-root>`.</action>
  <verify>head -n 25 README.md</verify>
  <acceptance_criteria>
    - `README.md` first heading is not exactly `# SceneDeck` alone (must include MetroVision or state rebrand explicitly)
    - `README.md` does not instruct `cd Claude` (grep exits 1 for `cd Claude`)
  </acceptance_criteria>
  <done>README matches product name</done>
</task>

<task type="auto">
  <name>AGENTS and Kiln config section</name>
  <files>AGENTS.md,.kiln/docs/codebase-state.md</files>
  <read_first>AGENTS.md tail “Config” or “Known Issues”</read_first>
  <action>In `AGENTS.md` **Key Files** or endmatter, if “package.json still named scenedeck” appears, remove or update. In `.kiln/docs/codebase-state.md` M1 deliverable about package name, mark complete or update to metrovision / metrovision-worker per actual files.</action>
  <verify>rg -i "scenedeck-worker|name.*scenedeck" AGENTS.md .kiln/docs/codebase-state.md</verify>
  <acceptance_criteria>
    - `AGENTS.md` does not state package.json is still named `scenedeck` as a known issue unless factually true after edits
    - `.kiln/docs/codebase-state.md` M1 package-name bullet reflects `metrovision` + `metrovision-worker` state
  </acceptance_criteria>
  <done>No stale naming debt in agent docs</done>
</task>

</tasks>

<verification>
- [ ] `rg 'scenedeck-worker' --glob package.json` — only absent or updated to metrovision-worker
</verification>

<success_criteria>
- Canonical naming documented and package.json files match
</success_criteria>

<output>
After completion, create `01-04-SUMMARY.md`
</output>
