---
phase: 03-security-exposure
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/llm-route-gate.ts
  - src/app/api/rag/route.ts
  # Note: `src/app/api/agent/chat/route.ts` gated at the time; route later removed.
autonomous: true
requirements:
  - REQ-SEC-01
user_setup: []
must_haves:
  truths:
    - "When METROVISION_LLM_GATE_SECRET is set, rag requires x-metrovision-llm-gate (was also applied to chat while that route existed)"
    - "When secret unset, routes behave as before for local dev"
  artifacts:
    - src/lib/llm-route-gate.ts
---

<objective>
Add optional shared-secret gate for `POST /api/rag` via `METROVISION_LLM_GATE_SECRET` and header `x-metrovision-llm-gate`. *(Chat route received the same gate when it existed.)*
</objective>
