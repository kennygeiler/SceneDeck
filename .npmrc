# Vercel rejects Serverless Function bundles that trace pnpm's symlinked virtual store.
# Hoisted node_modules matches npm-style layout and avoids "invalid deployment package" deploy failures.
node-linker=hoisted
